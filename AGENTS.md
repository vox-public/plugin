# vox.ai public Plugin

This is a separate repository from the MCP server. Always write `vox.ai`.
Keep Codex and Claude manifests aligned and build both external and embedded bundles from the same skills and references.
Run `python3 scripts/build.py` after changes. Do not add credentials, server implementation, local absolute paths, or internal-only knowledge links.
Keep public documentation focused on installation and product use. Keep research provenance, internal design IDs and deployment notes outside this repository. Skills must inspect actual MCP schemas.
